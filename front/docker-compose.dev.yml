services:
  roomly_frontend:
    build: .
    env_file:
      - .env
      - .env.prod
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev
    ports:
      - '3030:3030'
    networks:
      - app-network
