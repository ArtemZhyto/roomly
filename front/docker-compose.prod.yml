services:
  roomly_frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    env_file:
      - .env
      - .env.prod
    ports:
      - '3030:3030'
    networks:
      - app-network
